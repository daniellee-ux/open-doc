import type { OpendocConfig } from '@opendoc/core';

const config: OpendocConfig = {
  // Defaults are fine for the demo. Examples:
  // docsDir: 'docs',
  // page: { size: 'A4', margin: '1in' },
};

export default config;
